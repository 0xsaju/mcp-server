# Docker Compose Environment Variables for DeenMate n8n Integration

# PostgreSQL Configuration
POSTGRES_PASSWORD=deenmate_postgres_2024

# n8n Configuration
N8N_ENCRYPTION_KEY=deenmate_encryption_key_2024_secure
WEBHOOK_URL=http://localhost:5678

# MCP Bridge Configuration
MCP_HOST=0.0.0.0
MCP_PORT=8080

# Optional: External URLs (if running on remote server)
# WEBHOOK_URL=http://your-server-ip:5678
# Replace 'your-server-ip' with your actual server IP

# DeenMate API Configuration (if you have existing APIs)
# DEENMATE_API_URL=http://your-deenmate-backend:3000
# DEENMATE_API_KEY=your-api-key

# Notification Configuration (for workflows)
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token
# SLACK_WEBHOOK_URL=your-slack-webhook-url
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
